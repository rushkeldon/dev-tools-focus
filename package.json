{
    "name": "dev-tools-focus",
    "version": "1.0.0",
    "description": "A chrome extension that brings a dev tools window to the top when its corresponding tab is focused.",
    "main": "background.js",
    "scripts": {
        "build": "npm run clean && tsc && cp src/manifest.json dist",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.268",
        "typescript": "^4.5.2"
    }
}
